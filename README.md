# gh-pages — Poster Archive

This branch hosts daily Slack poster images for `ask-ai-daily-automation`. Do not commit other content here. Pruning runs daily via `.github/workflows/prune-posters.yml` on `main` and deletes PNGs older than 90 days. See `scripts/README_poster_publisher.md` on `main` for the full operational contract.
